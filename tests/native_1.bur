
decl start = time.clock();

decl oneHalf = math.sin(math.pi / 6);
decl thing = math.sin(math.pi / 3);

print("One half is equal to: ");
print(oneHalf);

print("Root three over two is equal to: ");
print(thing);

decl end = time.clock();
decl elapsed = end - start;

print("time: ");
print(elapsed);
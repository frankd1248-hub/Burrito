

fn fib(n) {
    if (n == 1 or n == 2) return 1;
    return fib(n-1) + fib(n-2);
}

decl start = time.clock();
print("%d\n", fib(35));
decl elapsed = time.clock() - start;

print("Time: %d\n", elapsed);